<div class="container">
  <div class="row">
    <div class="col-lg-3 d-flex flex-column align-items-center">
      <h1 class="font-weight-bold">Artists detail</h1>
      <%= render 'index', artist: @artist %>
    </div>
  </div>
</div>

<% @users.each do |user| %>
<%= user.name %>
<% end %>