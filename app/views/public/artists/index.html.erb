<div class="container">
  <div class="row">
    <div class="col-lg-12 d-flex justify-content-center">
      <div class="glitch" data-text="Artist">Artist</div>
    </div>
    <% @artists.each do |artist| %>
    <div class="col-lg-3 d-flex flex-column align-items-center">
      <%= render 'index', artist: artist %>
    </div>
    <% end %>
  </div>
  <%= paginate @artists %>
</div>


